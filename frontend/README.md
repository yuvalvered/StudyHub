# StudyHub Frontend

פלטפורמת שיתוף לימודים חכמה לסטודנטים - ממשק משתמש

## טכנולוגיות

- **Next.js 16** - React framework
- **TypeScript** - Type safety
- **Tailwind CSS 4** - Styling framework
- **React 19** - UI library

## התקנה והרצה

### דרישות מקדימות
- Node.js 18+ מותקן במחשב
- npm או yarn

### התקנת תלויות
```bash
npm install
```

### הרצת שרת פיתוח
```bash
npm run dev
```

האתר יהיה זמין בכתובת: [http://localhost:3000](http://localhost:3000)

### בנייה לפרודקשן
```bash
npm run build
npm start
```

## מבנה הפרויקט

```
frontend/
├── app/                      # Next.js App Router pages
│   ├── page.tsx             # Root page - מפנה ל-/login
│   ├── layout.tsx           # Root layout with RTL support
│   ├── globals.css          # Global styles
│   │
│   ├── login/               # עמוד התחברות ✅
│   │   └── page.tsx
│   │
│   ├── register/            # עמוד רישום (בבנייה)
│   │   └── page.tsx
│   │
│   ├── dashboard/           # דף הבית (בבנייה)
│   │   └── page.tsx
│   │
│   ├── courses/             # עמודי קורסים (בבנייה)
│   │   ├── page.tsx         # רשימת קורסים
│   │   └── [id]/
│   │       └── page.tsx     # פרטי קורס ספציפי
│   │
│   └── profile/             # פרופיל אישי (בבנייה)
│       └── page.tsx
│
├── components/              # Reusable components
│   └── Logo.tsx            # StudyHub logo component
├── public/                 # Static assets
├── tailwind.config.js     # Tailwind configuration
├── tsconfig.json          # TypeScript configuration
└── next.config.js         # Next.js configuration
```

## עמודים זמינים

### ✅ עמוד התחברות (`/login`)
- שדה שם משתמש
- שדה סיסמה
- כפתור התחברות
- קישור ליצירת משתמש חדש
- קישור לשכחתי סיסמה
- תמיכה מלאה בעברית (RTL)

### 🚧 עמודים בבנייה

- `/` - מפנה אוטומטית ל-`/login`
- `/register` - רישום משתמש חדש
- `/dashboard` - דף הבית לאחר התחברות
- `/courses` - רשימת קורסים
- `/courses/[id]` - פרטי קורס ספציפי (דינמי)
- `/profile` - פרופיל אישי

## צבעים (Blue Theme)

הפרויקט משתמש בפלטת צבעים כחולה:
- Primary: גוני כחול (#3b82f6)
- Secondary: גוני אפור כהה
- Background: גוני אפור בהיר

## הערות פיתוח

- הקוד כולל הערות מפורטות בעברית להבנה טובה יותר
- כל קומפוננטה מתועדת עם JSDoc
- תמיכה מלאה ב-RTL לעברית
- עיצוב רספונסיבי לכל המכשירים

## חיבור ל-Backend

כרגע דף ההתחברות עובד במצב demo. לחיבור ל-backend:
1. פתח את `app/login/page.tsx`
2. הסר את ההערה מקריאת ה-API בפונקציה `handleLogin`
3. עדכן את ה-URL לכתובת ה-backend שלך

```typescript
const response = await fetch('YOUR_BACKEND_URL/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username, password }),
})
```

## תמיכה

לשאלות או בעיות, פנה למפתחים.
